---
phase: 01-validation-gates-stack-lock
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/gate_0b_log.csv
  - evals/gate_0b_results.md
  - prompts/advice_v0_gate0b.txt
autonomous: false
requirements: [GATE-02]
user_setup:
  - service: groq
    why: "Groq API key needed for Gate 0b WoZ advice calls (Llama 3.3 70B, free tier, 30 RPM / 1000 RPD). No CC required."
    env_vars:
      - name: GROQ_API_KEY
        source: "console.groq.com → sign in → API keys → Create key"
  - service: gemini_api
    why: "Fallback for Groq 429 during Gate 0b. Also free, no CC required."
    env_vars:
      - name: GOOGLE_AI_STUDIO_API_KEY
        source: "aistudio.google.com → sign in → API keys → Create key"
must_haves:
  truths:
    - "Founder has logged ≥2 meals/day × 14 consecutive days = ≥28 meal-advice pairs"
    - "Each meal: macros derived from dish_decomposition.json + ifct_lookup.json (NOT HealthifyMe or USDA)"
    - "Each advice reply generated via Groq/Gemini playground using the advice_v0_gate0b.txt prompt"
    - "Each advice reply scored same-day on 4/4 rubric (a: specific food, b: quantity in g, c: ₹ cost, d: log reference)"
    - "≥70% of advice replies score 4/4 (or ≥70% score 3/4+ with ₹-missing-only documented as infra gap)"
    - "gate_0b_log.csv committed with all 28+ rows"
    - "gate_0b_results.md summarizes pass rate + prompt iteration log"
    - "Prompt iterated if pass rate drops below 70% in first 5 days"
  artifacts:
    - path: "evals/gate_0b_log.csv"
      provides: "28+ meal-advice rows with 4/4 rubric scores"
      contains: "score_a,score_b,score_c,score_d,total_4_4,pass"
    - path: "evals/gate_0b_results.md"
      provides: "Gate 0b pass/fail summary + pass rate + rubric breakdown"
      contains: "pass rate"
    - path: "prompts/advice_v0_gate0b.txt"
      provides: "WoZ advice prompt used during Gate 0b; becomes v0.1 baseline for Phase 2"
      contains: "Bhog"
  key_links:
    - from: "prompts/advice_v0_gate0b.txt"
      to: "Phase 2 prompts/advice_v0.json"
      via: "Gate 0b prompt becomes the versioned baseline prompt committed in Phase 2"
      pattern: "Bhog"
    - from: "data/dish_decomposition.json"
      to: "evals/gate_0b_log.csv"
      via: "Gate 0b macros must derive from dish_decomposition.json — not any other source"
      pattern: "dish_id"
---

<objective>
Founder runs 14 days of personal meal logging using a Wizard-of-Oz manual advice workflow (no backend exists yet), scores each advice reply same-day on the 4/4 rubric, iterates the prompt if needed, and documents gate passage in evals/gate_0b_results.md.

Purpose: Gate 0b validates that the AI advice engine can reach the moat quality bar (4/4 rubric: specific food + quantity + ₹ cost + log reference) before any production code is written. This is the WoZ QA Gate's first instance. The 14-day log also generates the prompt-engineering dataset that informs Phase 2 prompt versioning. This plan runs as a long pole across the entire 14-day Phase 1 window — start immediately on Day 1.

Output: evals/gate_0b_log.csv (28+ rows), evals/gate_0b_results.md (pass/fail), prompts/advice_v0_gate0b.txt (iterated prompt baseline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-validation-gates-stack-lock/01-CONTEXT.md
@.planning/phases/01-validation-gates-stack-lock/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Gate 0b log schema, v0 advice prompt, and macro calculator one-liner</name>
  <files>evals/gate_0b_log.csv, prompts/advice_v0_gate0b.txt</files>
  <read_first>
    Read .planning/phases/01-validation-gates-stack-lock/01-RESEARCH.md section "Gate 0b — Execution Mechanics" fully. It contains the exact WoZ mechanic, the prompt template, the 4/4 rubric definitions, and the 3/4 conditional-pass guidance. The prompt template in that section is the canonical starting point for prompts/advice_v0_gate0b.txt.

    Read data/dish_decomposition.json to verify it has been updated by Plan 01 (verified_by != null). If Plan 01 is not yet complete, note this dependency in the log — the macro calculator cannot use verified dish data until Plan 01 finishes.
  </read_first>
  <action>
    Part A — Create evals/gate_0b_log.csv with headers only:

    Headers: date,meal_number,dish_id,portion_g,derived_kcal,derived_protein_g,derived_carb_g,derived_fat_g,advice_text,score_a,score_b,score_c,score_d,total_4_4,pass,notes

    Column definitions:
    - date: YYYY-MM-DD of the meal
    - meal_number: 1, 2, 3 (meals in a day; minimum 2/day)
    - dish_id: must match a key in data/dish_decomposition.json
    - portion_g: estimated portion in grams (founder estimate — this is different from Gate 0a; no scale needed for Gate 0b daily logging)
    - derived_kcal/protein_g/carb_g/fat_g: computed from dish_decomposition.json + ifct_lookup.json via the macro calculator
    - advice_text: the full advice reply from Groq/Gemini playground (paste verbatim)
    - score_a: 0 or 1 — specific food cited by name
    - score_b: 0 or 1 — specific quantity in g or units cited
    - score_c: 0 or 1 — ₹ cost cited; if veg_protein_prices table not yet built, score 0 and add "infra_gap" in notes
    - score_d: 0 or 1 — reference to user's recent meal log (last 3 days)
    - total_4_4: sum of a+b+c+d (0-4)
    - pass: 1 if total_4_4 == 4; for conditional pass (total_4_4 == 3 AND notes == "infra_gap"), enter "3/4-conditional"
    - notes: any scoring ambiguity or infra-gap flag

    Part B — Write prompts/advice_v0_gate0b.txt:

    This is the WoZ prompt the founder pastes into Groq playground (llama-3.3-70b-versatile) or Gemini AI Studio for each meal. Use the template from RESEARCH.md Gate 0b section exactly, with [TEMPLATE_VARS] clearly marked for the founder to fill per meal:

    ---
    You are Bhog, an AI nutrition coach for Indian youth. The user's goal is muscle gain.

    Target: 1.8 g protein/kg body weight/day = [TARGET_PROTEIN_G]g protein. Calorie target: [TARGET_KCAL] kcal.

    Today's meals so far:
    [TODAY_MEALS_WITH_MACROS — list each meal: dish, portion, kcal, protein]

    Last 3 days average: [AVG_PROTEIN_3D]g protein/day, [AVG_KCAL_3D] kcal/day.

    Current meal just logged: [DISH_NAME], ~[PORTION_G]g, ~[MEAL_KCAL] kcal, ~[MEAL_PROTEIN_G]g protein.

    User is vegetarian. Daily food budget: ₹[BUDGET]/day.

    Veg protein reference (₹/g protein — fill from veg_protein_prices_seed.csv when available):
    - Soya chunks: ₹0.15/g protein
    - Toor dal: ₹0.20/g protein
    - Eggs (ovo-veg): ₹0.30/g protein
    - Paneer: ₹0.40/g protein
    - Roasted chana: ₹0.18/g protein
    [Add more from veg_protein_prices_seed.csv once collected]

    Give ONE specific, actionable piece of advice in 1-2 sentences. The advice MUST include:
    (a) a named Indian food (e.g., "50g soya chunks", not "add protein")
    (b) a specific gram amount or count (e.g., "50g" or "2 katoris (300g)")
    (c) approximate ₹ cost (e.g., "≈₹6", or "costs about ₹8")
    (d) reference to the user's recent log (e.g., "yesterday's lunch had only 12g protein")

    Rules: Only Indian foods. No Western diet recommendations. No medical claims. Never use: treat/cure/diagnose/medicine/drug. Advice must be achievable on an Indian food budget.
    ---

    Part C — Write the macro calculator one-liner as a comment at the top of the log CSV:

    # MACRO CALCULATOR: python -c "
    # import json
    # decomp = json.load(open('data/dish_decomposition.json'))
    # ifct = json.load(open('data/ifct_lookup.json'))
    # dish_id = 'dal_toor_tadka'  # change this
    # portion_g = 150  # change this
    # dish = decomp['dishes'][dish_id]
    # serving_g = float(dish['serving_label'].split('(~')[1].replace('g cooked)','').replace('g)',''))
    # scale = portion_g / serving_g
    # totals = {'kcal':0, 'protein':0, 'carb':0, 'fat':0}
    # for ing in dish['ingredients']:
    #     entry = ifct.get('ingredients',{}).get(ing['ifct_id'], {})
    #     for k in totals:
    #         totals[k] += entry.get(k, 0) * ing['g'] * scale / 100
    # print({k: round(v,1) for k,v in totals.items()})
    # "

    This one-liner lets the founder compute macros for any dish + portion in seconds during 14-day logging.

    Commit: git add evals/gate_0b_log.csv prompts/advice_v0_gate0b.txt && git commit -m "feat(phase-1): Gate 0b log schema + WoZ advice prompt v0"
  </action>
  <verify>
    ls evals/gate_0b_log.csv prompts/advice_v0_gate0b.txt
    head -1 evals/gate_0b_log.csv — must show the header row with score_a,score_b,score_c,score_d,total_4_4,pass
    grep -c "score_a" evals/gate_0b_log.csv — returns 1 (header row only; no data yet)
    grep "Bhog" prompts/advice_v0_gate0b.txt — returns the first line containing "Bhog"
    git log --oneline -1 shows the Gate 0b setup commit
  </verify>
  <done>evals/gate_0b_log.csv has correct headers. prompts/advice_v0_gate0b.txt is the WoZ prompt with all template vars marked. Both committed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Founder runs 14-day WoZ meal log + same-day scoring</name>
  <what-built>Claude has created the log schema, WoZ prompt, and macro calculator. The founder now runs the 14-day logging protocol. This is the long pole of Phase 1 — start on Day 1 and continue daily.</what-built>
  <how-to-verify>
    DAILY PROTOCOL (repeat every day for 14 days, minimum 2 meals/day):

    Step 1 — Log the meal:
    - Identify the dish eaten. Look it up in data/dish_decomposition.json. Get the dish_id.
    - Estimate portion in grams (founder's best estimate — kitchen scale optional here, not required).
    - Run the macro calculator one-liner (in the CSV comment header) with dish_id + portion_g.
    - Record: date, meal_number, dish_id, portion_g, derived_kcal, derived_protein_g, derived_carb_g, derived_fat_g.

    CRITICAL: Use dish_decomposition.json + ifct_lookup.json as the macro source. Do NOT use HealthifyMe, USDA FoodData, or any other source — Gate 0b validates this specific pipeline (Pitfall E-03).

    Step 2 — Generate the advice:
    - Open Groq playground: console.groq.com → Playground. Select model: llama-3.3-70b-versatile.
    - Fill in the template vars in prompts/advice_v0_gate0b.txt:
      - TARGET_PROTEIN_G: your body weight × 1.8 (e.g., 70kg → 126g)
      - TARGET_KCAL: your maintenance calories (e.g., 2200 for a 70kg active male)
      - TODAY_MEALS_WITH_MACROS: list meals logged so far today
      - AVG_PROTEIN_3D / AVG_KCAL_3D: average of last 3 days from gate_0b_log.csv
      - DISH_NAME, PORTION_G, MEAL_KCAL, MEAL_PROTEIN_G: current meal
      - BUDGET: your daily food budget
    - Paste the filled prompt into Groq playground. Copy the response verbatim into advice_text column.
    - Fallback: if Groq is 429'd, use Gemini AI Studio text (same prompt).

    Step 3 — Score same-day (do NOT batch at day 14 — Pitfall E-04):
    - score_a (0 or 1): Is a specific Indian food named? "50g soya chunks" = 1. "eat more protein" = 0.
    - score_b (0 or 1): Is a specific quantity in g or units given? "50g" or "2 katoris" = 1. "some" = 0.
    - score_c (0 or 1): Is a ₹ cost mentioned? "≈₹6" or "costs about ₹8" = 1. No ₹ mention = 0.
      If score_c = 0 because veg_protein_prices not yet built: add "infra_gap" in notes. Score: 0 but flag.
    - score_d (0 or 1): Is there a reference to the user's recent log? "yesterday you had only 12g protein" = 1.
    - total_4_4 = a + b + c + d.
    - pass = 1 if total_4_4 == 4; "3/4-conditional" if total_4_4 == 3 AND notes == "infra_gap".

    PROMPT ITERATION RULE:
    Check pass rate after every 5 meals (not every meal). If pass rate is below 70% after 5 meals (≤3/5 passing), iterate the prompt immediately:
    - Common fix for score_a fails: add "NEVER say 'protein' generically — always name the specific food" to the prompt.
    - Common fix for score_d fails: add "ALWAYS cite one specific meal from the last 3 days by name" to the prompt.
    - Common fix for score_c fails when prices table is available: add the full prices table to the prompt context.
    - Commit each prompt version: git add prompts/ && git commit -m "chore(gate-0b): iterate advice prompt — day N pass rate X%"

    OPEN QUESTION (resolved per RESEARCH.md):
    Does 3/4 with ₹-cost missing count as conditional pass while the price table is being built?
    YES — score as "3/4-conditional" in the pass column, log "infra_gap" in notes. If ≥70% of meals reach 4/4 OR ≥70% reach 3/4+ with ₹-missing-only, gate passes. Aim to complete Plan 03 (veg_protein_prices) by Day 3 to minimize conditional scoring.

    AFTER DAY 14 — compute results:
    python -c "
    import csv
    rows = [r for r in csv.DictReader(open('evals/gate_0b_log.csv')) if r.get('date')]
    total = len(rows)
    pass4 = sum(1 for r in rows if r.get('pass') == '1')
    pass3c = sum(1 for r in rows if r.get('pass') == '3/4-conditional')
    print(f'Total meals: {total}')
    print(f'4/4 passes: {pass4} ({pass4/total:.0%})')
    print(f'3/4-conditional: {pass3c} ({pass3c/total:.0%})')
    rate_strict = pass4/total
    rate_inclusive = (pass4+pass3c)/total
    print(f'Strict pass rate (4/4 only): {rate_strict:.0%}')
    print(f'Inclusive pass rate (3/4+ with infra_gap): {rate_inclusive:.0%}')
    gate_pass = rate_strict >= 0.70 or rate_inclusive >= 0.70
    print(f'Gate 0b: {\"PASS\" if gate_pass else \"FAIL\"}')
    "

    Write evals/gate_0b_results.md with:
    - Total meals logged
    - Strict pass rate (4/4 only)
    - Inclusive pass rate (3/4 conditional)
    - Gate 0b verdict: PASS or FAIL
    - Prompt version history (v0 → v0.1 → etc. with dates and what changed)
    - Top 3 failure patterns observed
    - Rubric dimension breakdown: % passing each of a, b, c, d separately

    git add evals/gate_0b_log.csv evals/gate_0b_results.md prompts/ && git commit -m "feat(phase-1): Gate 0b — 14-day WoZ log complete; pass rate [X]%"
  </how-to-verify>
  <resume-signal>Type "gate 0b complete" when all 14 days are logged, gate_0b_results.md is written, and files are committed. Paste the strict and inclusive pass rates.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Founder meal data → gate_0b_log.csv | Personal eating data; no user PII; founder's own meals only |
| Groq/Gemini playground → advice_text | External API call with meal context; prompt contains no user PII (founder's own meals) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | gate_0b_log.csv contains founder's personal meal data | accept | Internal test artifact, not user data; no DPDP obligation for founder's own testing data; gitignore if founder prefers privacy |
| T-05-02 | Tampering | Using HealthifyMe/USDA macros instead of dish_decomposition.json | mitigate | Pitfall E-03 explicitly called out; macro calculator one-liner in CSV header makes dish_decomposition.json easy to use; scoring notes column surfaces if another source was used |
| T-05-03 | Repudiation | Prompt not versioned — can't attribute which prompt generated which advice | mitigate | prompts/advice_v0_gate0b.txt committed; each iteration = new commit with version in message; gate_0b_results.md logs version history |
| T-05-04 | Denial of Service | Groq 429 rate limit mid-day | accept | Fallback: Gemini AI Studio text-only (same prompt); 30 RPM / 1000 RPD on Groq easily covers 2-3 manual calls/day |
</threat_model>

<verification>
Gate 0b passes when:
1. evals/gate_0b_log.csv has ≥28 rows with date, dish_id, derived_protein_g, advice_text, and all 4 score columns populated.
2. evals/gate_0b_results.md exists and states Gate 0b: PASS.
3. Strict pass rate ≥70% (4/4) OR inclusive pass rate ≥70% (3/4+ with ₹-missing-only flagged as infra_gap).
4. All files committed to git.

python -c "
import csv
rows = [r for r in csv.DictReader(open('evals/gate_0b_log.csv')) if r.get('date')]
assert len(rows) >= 28, f'Need ≥28 meals, got {len(rows)}'
scored = [r for r in rows if r.get('total_4_4')]
assert len(scored) == len(rows), 'Some rows not scored'
pass4 = sum(1 for r in rows if r.get('pass') == '1')
pass3c = sum(1 for r in rows if r.get('pass') == '3/4-conditional')
rate = (pass4+pass3c)/len(rows)
assert rate >= 0.70, f'Pass rate {rate:.0%} < 70%'
print(f'Gate 0b PASS — {rate:.0%} pass rate ({len(rows)} meals)')
"
</verification>

<success_criteria>
≥28 meal-advice pairs logged across 14 days. Each meal scored same-day on 4/4 rubric. ≥70% achieve 4/4 (strict) or 3/4-conditional (inclusive with ₹-infra-gap). gate_0b_results.md states PASS. Prompt iterated as needed during the 14-day window. prompts/advice_v0_gate0b.txt is the versioned baseline for Phase 2.
</success_criteria>

<output>
Create .planning/phases/01-validation-gates-stack-lock/01-05-SUMMARY.md when done.
</output>
