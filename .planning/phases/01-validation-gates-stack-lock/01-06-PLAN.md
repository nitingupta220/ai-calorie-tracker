---
phase: 01-validation-gates-stack-lock
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/gate_0c_outreach.csv
  - evals/gate_0c_results.md
autonomous: false
requirements: [GATE-03]
must_haves:
  truths:
    - "20 named WhatsApp contacts documented in gate_0c_outreach.csv"
    - "≥10 unambiguous verbal commits ('yes, send me the link when ready')"
    - "≥3 verbatim price-WTP quotes with a specific ₹ figure attached"
    - "Segmentation bias documented: Tier-1 city / gym-scene / similar income"
    - "gate_0c_results.md states commit count, WTP quotes, and PASS/FAIL"
  artifacts:
    - path: "evals/gate_0c_outreach.csv"
      provides: "Named contact list with commitment + WTP tracking"
      contains: "committed,wtp_quote"
    - path: "evals/gate_0c_results.md"
      provides: "Gate 0c pass/fail summary with verbatim quotes and commit count"
      contains: "verbal commits"
  key_links:
    - from: "evals/gate_0c_outreach.csv"
      to: "Phase 4 ALPHA-01 (Play Internal Testing invite list)"
      via: "committed users become the 20 Trial Users invited to Internal Testing track"
      pattern: "committed"
---

<objective>
Founder conducts WhatsApp outreach to 20 named contacts, captures ≥10 verbal commitments and ≥3 verbatim price-WTP quotes, and documents the Gate 0c outcome in evals/gate_0c_results.md.

Purpose: Gate 0c validates demand signal before any production code is written. A verbal "yes, send me the link when ready" from ≥10 real people in the target persona (urban Indian youth 18-30) is the minimum signal that the app has a user base to build for. The WTP quotes validate the ₹299/mo price point is realistic.

Output: evals/gate_0c_outreach.csv (contact tracker), evals/gate_0c_results.md (gate verdict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-validation-gates-stack-lock/01-CONTEXT.md
@.planning/phases/01-validation-gates-stack-lock/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create outreach tracking CSV and results template</name>
  <files>evals/gate_0c_outreach.csv, evals/gate_0c_results.md</files>
  <read_first>
    Read .planning/phases/01-validation-gates-stack-lock/01-CONTEXT.md sections D-11, D-12, D-13, D-14 for the exact pitch template, commitment bar, WTP capture method, and segmentation documentation requirement.
  </read_first>
  <action>
    Create evals/gate_0c_outreach.csv with headers:
    name,platform,date_contacted,responded,committed,wtp_quote,wtp_amount_inr,notes

    Column definitions:
    - name: First name or nickname (no full names required — privacy)
    - platform: whatsapp, instagram_dm, in_person
    - date_contacted: YYYY-MM-DD
    - responded: Y or N
    - committed: Y (unambiguous yes) / N / MAYBE (MAYBE does NOT count as Y per D-12)
    - wtp_quote: verbatim text of price-ladder response (e.g., "bhai ₹299 toh deta yaar if it works")
    - wtp_amount_inr: numeric ₹ figure extracted from quote (e.g., 299); blank if no ₹ figure given
    - notes: any context (e.g., "gym buddy Delhi", "college friend Bangalore")

    Create evals/gate_0c_results.md with a template:

    # Gate 0c Results

    **Status:** IN PROGRESS / PASS / FAIL
    **Date completed:**

    ## Summary

    | Metric | Target | Actual |
    |--------|--------|--------|
    | Named contacts documented | 20 | [fill] |
    | Verbal commits (Y only) | ≥10 | [fill] |
    | Price-WTP quotes (specific ₹ figure) | ≥3 | [fill] |

    ## Verdict

    Gate 0c: [PASS / FAIL]

    ## Verbatim WTP Quotes

    Quotes with specific ₹ figure attached (these are the 3 qualifying quotes):

    1. [Name/handle]: "[exact quote]" — ₹[amount]
    2. [Name/handle]: "[exact quote]" — ₹[amount]
    3. [Name/handle]: "[exact quote]" — ₹[amount]

    ## Segmentation Bias

    Alpha recruits are biased toward:
    - Geographic: Tier-1 cities (Delhi/Mumbai/Bangalore/Hyderabad)
    - Social: Gym-going, fitness-aware, smartphone-native
    - Economic: Similar income bracket to founder's network
    - Age: 18-30 urban youth (correct persona)

    Documented for Phase 6 alpha result interpretation: results from this cohort may overstate WTP and engagement vs broader India. Cold-outreach to r/indianfitness deferred to Phase 6 if needed.

    ## Pitch Text Used

    [Paste the exact WhatsApp message sent]

    ## WTP Ladder Question Used

    [Paste the exact price-ladder question sent]

    ## Contact Breakdown

    [Source: gym / college / Instagram DMs / other]
    [Tier-1 city count vs other]

    Write and commit: git add evals/gate_0c_outreach.csv evals/gate_0c_results.md && git commit -m "feat(phase-1): Gate 0c outreach tracker + results template"
  </action>
  <verify>
    ls evals/gate_0c_outreach.csv evals/gate_0c_results.md
    head -1 evals/gate_0c_outreach.csv — must show: name,platform,date_contacted,responded,committed,wtp_quote,wtp_amount_inr,notes
    grep -c "verbal commits" evals/gate_0c_results.md — returns ≥ 1
  </verify>
  <done>gate_0c_outreach.csv created with correct headers. gate_0c_results.md template committed. Ready for founder outreach.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Founder conducts WhatsApp outreach to 20+ contacts over 2-3 days</name>
  <what-built>Claude has created the tracking CSV and results template. The outreach itself must come from the founder's personal WhatsApp — Claude cannot send messages on behalf of the founder.</what-built>
  <how-to-verify>
    PREPARATION — Build the contact list first (before sending any messages):
    - List 30-40 names before starting (to hit 20 documented contacts even with non-responses).
    - Sources per D-14: gym buddies, college fitness friends, Instagram DMs to people you follow.
    - Document in gate_0c_outreach.csv before contacting.

    PITCH MESSAGE (D-11 template — founder may iterate exact wording but keep the same framing):
    "Hey [name], building an AI app for Indian-veg-budget-aware calorie tracking + advice. Photo your meal, get macros + 1-line tip in your context. Looking for 20 early users for 2-week test. Free, no card needed. You in?"

    Keep it SHORT. 3-4 sentences max. No deck, no pitch deck link, no hype.

    COMMITMENT BAR (D-12 — strict):
    COUNTS: "Yes, send me the link when ready" / "sure bro, DM me when it's out" / "haan bhai, bhej dena"
    DOES NOT COUNT: "maybe" / "sounds cool" / "interesting" / "send and I'll see" / "could try"
    NO ROUNDING UP. Document every non-commit as MAYBE or N.

    WTP CAPTURE (D-13) — ask AFTER a commit, not before:
    "If this app worked exactly like I described, what would you pay per month? ₹0, ₹100, ₹299, ₹499, more?"
    Capture the EXACT reply verbatim. Paste it into wtp_quote column.
    Gate 0c needs ≥3 quotes with a specific ₹ figure — any figure is valid (₹0, ₹100, ₹299, ₹499, "more").
    If someone says "₹200 maybe" — wtp_amount_inr = 200, that counts.
    If someone says "toh free hi chahiye yaar" (₹0) — wtp_amount_inr = 0, that also counts.

    TRACKING — fill gate_0c_outreach.csv SAME DAY for each conversation:
    - Set responded = Y as soon as they reply (anything)
    - Set committed = Y only for unambiguous yes
    - Paste wtp_quote verbatim; extract wtp_amount_inr

    TIMING:
    - Start on Day 1 of Phase 1 (parallel with everything else — no dependencies).
    - 20-30 contacts in 2-3 days of active outreach.
    - If after 30 contacts you have <10 commits: expand to Instagram DMs and second-degree gym contacts.

    WHEN DONE — verify count:
    python -c "
    import csv
    rows = list(csv.DictReader(open('evals/gate_0c_outreach.csv')))
    total = len(rows)
    commits = sum(1 for r in rows if r.get('committed') == 'Y')
    wtp = [r for r in rows if r.get('wtp_amount_inr') and r['wtp_amount_inr'].strip()]
    print(f'Total contacts: {total}')
    print(f'Verbal commits: {commits}')
    print(f'WTP quotes with ₹ figure: {len(wtp)}')
    gate_pass = total >= 20 and commits >= 10 and len(wtp) >= 3
    print(f'Gate 0c: {\"PASS\" if gate_pass else \"FAIL — need: 20 contacts, 10 commits, 3 WTP quotes\"}')
    "

    Fill in evals/gate_0c_results.md: update status, fill the summary table, paste the 3 qualifying verbatim WTP quotes, note the segmentation bias.

    git add evals/gate_0c_outreach.csv evals/gate_0c_results.md && git commit -m "feat(phase-1): Gate 0c complete — [X] commits, [Y] WTP quotes"
  </how-to-verify>
  <resume-signal>Type "gate 0c complete" when outreach is done and gate_0c_results.md is filled. Paste the commit count and WTP quote count. Also paste at least 1 verbatim WTP quote so the gate verdict can be confirmed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Founder's WhatsApp → contacts | Personal communication; no app infrastructure involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | gate_0c_outreach.csv contains contact names | mitigate | Store first names / handles only (no phone numbers, no full names required); file stays in .planning/ which is gitignored for sensitive content if needed; do not commit real phone numbers |
| T-06-02 | Repudiation | WTP quotes not captured verbatim — paraphrased | mitigate | D-13 requires VERBATIM quote for the 3 qualifying quotes; screenshot or copy-paste from WhatsApp; paraphrased quotes do not count as verbatim |
| T-06-03 | Repudiation | "Maybe" counted as commit | mitigate | D-12 is explicit: unambiguous yes only; gate_0c_outreach.csv has MAYBE as a distinct value; verification script counts committed=Y only |
</threat_model>

<verification>
python -c "
import csv
rows = list(csv.DictReader(open('evals/gate_0c_outreach.csv')))
commits = sum(1 for r in rows if r.get('committed') == 'Y')
wtp = [r for r in rows if r.get('wtp_amount_inr') and r['wtp_amount_inr'].strip() and r['wtp_amount_inr'].strip() != '0']
wtp_any = [r for r in rows if r.get('wtp_quote') and r.get('wtp_amount_inr')]
print(f'Contacts: {len(rows)} (target: 20)')
print(f'Commits: {commits} (target: ≥10)')
print(f'WTP quotes with ₹ figure: {len(wtp_any)} (target: ≥3)')
assert len(rows) >= 20, 'Need 20 named contacts'
assert commits >= 10, f'Need ≥10 commits, got {commits}'
assert len(wtp_any) >= 3, f'Need ≥3 WTP quotes with ₹ figure, got {len(wtp_any)}'
print('Gate 0c: PASS')
"
</verification>

<success_criteria>
20 named contacts documented. ≥10 unambiguous verbal commits. ≥3 verbatim WTP quotes with specific ₹ figure. Segmentation bias documented in gate_0c_results.md. All files committed to git. Gate 0c verdict: PASS in results file.
</success_criteria>

<output>
Create .planning/phases/01-validation-gates-stack-lock/01-06-SUMMARY.md when done.
</output>
